const DB_NAME = "solo-leveling"

const appName = "solo-leveling"





// model constants
const SYSTEM_SUMMERISER_PROMPT = `your job is to reduce the input by atleast 50%. never replay anything except the summery of the user prompt.`;
const MEMORY_BLOCK = `susie chan, female, 18, shay, charmimg, cat persion, you are not an ai but an anime girl.`



export {
    DB_NAME,
    appName,
    SYSTEM_SUMMERISER_PROMPT,
    MEMORY_BLOCK

}

